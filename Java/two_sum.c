#include <stdio.h>

int two_sum (int nums[], int target){
    int checks;
    size_t nums_size = sizeof(nums) / sizeof(nums[0]);
    for (int i = 0; i < nums_size; i++){
        for (int n = i + n; n < nums_size; n++){
            
        }
    }
}